package lessons.lesson03.homework

// 1. Название мероприятия — публичное, неизменяемое.
val eventName: String = "Monster Hackathon"

// 2. Дата проведения — публичная, может измениться при переносе.
var eventDate: String = "10 ноября 2026"

// 3. Место проведения — публичное, может измениться.
var eventLocation: String = "Москва, конференц-центр"

// 4. Подробный бюджет мероприятия — приватный, заранее утверждён.
private val detailedBudget: String =
    "Оборудование: 200000 руб.; питание: 150000 руб.; аренда: 150000 руб."

// 5. Количество участников — публичное, изменяется при регистрации.
var participantCount: Int = 0
    set(value) {
        require(value >= 0) {
            "Количество участников не может быть отрицательным"
        }
        field = value
    }

// 6. Длительность хакатона в часах — публичная, заранее установлена.
val hackathonDurationHours: Int = 48

// 7. Контакты и условия соглашений с поставщиками — приватные, могут обновляться.
private var supplierAgreements: String =
    "Поставщик питания: ООО «Обед»; условия: доставка три раза в день"

// 8. Текущее состояние хакатона — публичное, изменяемое.
var hackathonStatus: String = "Подготовка"

// 9. Список спонсоров — публичный, может пополняться.
var sponsors: String = "Компания «Альфа», компания «Бета»"

// 10. Общий бюджет мероприятия в рублях — приватный, заранее утверждён.
private val eventBudget: Int = 500000

// 11. Текущий уровень доступа к интернету — публичный, изменяемый.
var internetAccessLevel: String = "Полный доступ"

// 12. Транспортировка оборудования и координация поддержки — приватные данные.
private var equipmentLogistics: String =
    "Доставка оборудования накануне; распределение выполняет техническая команда"

// 13. Количество команд — публичное, изменяется при регистрации.
var teamCount: Int = 0
    set(value) {
        require(value >= 0) {
            "Количество команд не может быть отрицательным"
        }
        field = value
    }

// 14. Перечень задач — публичный, утверждён организаторами.
val taskList: String =
    "Разработать приложение; подготовить презентацию; показать прототип"

// 15. План эвакуации — публичный, должен быть доступен всем участникам.
val evacuationPlan: String =
    "Выйти через ближайший аварийный выход и пройти к точке сбора"

// 16. Список выделенного оборудования — публичный, заранее определён.
val availableEquipment: String =
    "20 ноутбуков, 5 мониторов, 2 проектора"

// 17. Список свободного оборудования — публичный, постоянно обновляется.
var freeEquipment: String =
    "20 ноутбуков, 5 мониторов, 2 проектора"

// 18. График питания — публичный, будет задан после выбора поставщика.
lateinit var mealSchedule: String

// 19. План действий при сбоях — приватная инструкция для организаторов.
private val failureResponsePlan: String =
    "Проверить питание; включить резервный интернет; вызвать техническую поддержку"

// 20. Список экспертов и жюри — публичный, может изменяться.
var expertsAndJury: String =
    "Анна Смирнова, Иван Петров"

// 21. Политика конфиденциальности — публичная, заранее утверждена.
val privacyPolicy: String =
    "Персональные данные используются только для организации мероприятия"

// 22. Приватные отзывы участников и зрителей — доступны организаторам, пополняются.
private var privateFeedback: String = "Отзывов пока нет"

// 23. Текущая температура в помещении в градусах Цельсия — публичная, изменяемая.
var roomTemperature: Int = 22

// 24. Данные мониторинга сети для отладки приложений командами — публичные, обновляемые.
var networkMonitoringData: String =
    "Задержка: 20 мс; скорость: 100 Мбит/с; потери пакетов: 0%"

// 25. Регулятор скорости интернета в процентах — приватный, изменяемый.
private var internetSpeedPercent: Int = 50
    set(value) {
        require(value in 0..100) {
            "Процент скорости должен быть от 0 до 100"
        }
        field = value
    }

// 26. Уровень освещения в процентах — публичный, изменяемый.
var lightingLevel: Int = 80
    set(value) {
        require(value in 0..100) {
            "Уровень освещения должен быть от 0 до 100"
        }
        field = value
    }

// 27. Лог событий мероприятия — приватный, пополняется по ходу мероприятия.
private var eventLog: String = "Началась подготовка к мероприятию"

// 28. Возможность получить медицинскую помощь на мероприятии — публичная, изменяемая.
var isMedicalHelpAvailable: Boolean = true

// 29. Внутренние планы и процедуры безопасности — приватные, заранее утверждены.
private val securityProcedures: String =
    "Проверка пропусков; дежурство охраны; порядок действий при чрезвычайных ситуациях"

// 30. Регистрационный номер мероприятия — публичный, неизменяемый.
val eventRegistrationNumber: String = "HACK-2026-001"

// 31. Максимально допустимый уровень шума в децибелах — публичный, заранее установлен.
val maxNoiseLevel: Int = 80

// 32. Индикатор превышения уровня шума — публичный, обновляется системой мониторинга.
var isNoiseLimitExceeded: Boolean = false
    private set

// 33. План взаимодействия с прессой — приватный, может корректироваться.
private var pressInteractionPlan: String =
    "Аккредитация журналистов; интервью с организаторами; итоговый пресс-релиз"

// 34. Публичная информация о проектах команд — собирается при первом обращении.
val teamProjectsInfo: String by lazy {
    // Учебная заглушка: здесь могла бы быть затратная сборка данных.
    "Команда «Код»: приложение для заметок; сборка успешна; пройдено 10 из 10 проверок"
}

// 35. Статус получения всех разрешений — публичный, обновляется организаторами.
var areAllPermitsReceived: Boolean = false
    private set

// 36. Доступность специализированного оборудования — публичная, изменяемая.
var isSpecialEquipmentAvailable: Boolean = true

// 37. Список партнёров мероприятия — публичный, может пополняться.
var eventPartners: String =
    "Технический университет, городской технопарк"

// 38. Итоговый отчёт с фото, видео и отзывами — публичный, будет задан после завершения.
lateinit var eventReport: String

// 39. Внутренние правила распределения призов — приватные, заранее утверждены.
private val internalPrizeRules: String =
    "При равенстве баллов решение принимает председатель жюри"

// 40. Список разыгрываемых призов — публичный, заранее определён.
val prizeList: String =
    "Ноутбук, планшет, наушники"

// 41. Контакты экстренных служб и медицинского персонала — для внутреннего пользования.
private val emergencyContacts: String =
    "Экстренные службы: 112; медицинский пост: внутренний номер 101"

// 42. Особые условия для участников с ограниченными возможностями — публичные.
val accessibilityConditions: String =
    "Пандус; лифт; доступные рабочие места; помощь волонтёров"

// 43. Общее настроение участников — публичное, обновляется по результатам опросов.
var participantMood: String = "Опрос ещё не проводился"

// 44. Подробный план хакатона — публичный, будет задан непосредственно перед началом.
lateinit var detailedHackathonPlan: String

// 45. Имя специального гостя — публичное после объявления, пока неизвестно.
lateinit var specialGuestName: String

// 46. Максимальная вместимость места проведения — публичная, неизменяемая.
val venueCapacity: Int = 200

// 47. Количество часов на работу над проектом для каждой команды — публичное, фиксированное.
val projectWorkHours: Int = 36